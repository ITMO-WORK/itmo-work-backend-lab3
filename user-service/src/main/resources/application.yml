spring:
  application:
    name: user-service
  profiles:
    active: ${PROFILE:dev}
  config:
    activate:
      on-profile: dev
    import: optional:configserver:http://localhost:${CONFIG_SERVER_PORT:9991}
server:
  port: 8081

jwt:
  secret: ${JWT_SECRET}
  access-ttl: ${JWT_TTL}